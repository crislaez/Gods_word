import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.godsword',
  appName: 'God´s word',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
